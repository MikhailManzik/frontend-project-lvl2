// import { type } from 'os';
import _ from 'lodash';
import { stringify } from '../utils.js';

export default (a) => {
  // console.log(typeof a);
  // console.log(Array.isArray(a));
  // console.log(a[0]);
  // console.log(stringify(a[0], '.', 1));
};
